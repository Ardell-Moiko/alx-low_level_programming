#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - check the code
 * @x: the integar to be taken.
 * Return: Always 0.
 */
int _abs(int x)
{
_putchar('0' + abs(x));
return (0);
}
